'use client';

/**
 * Main Chat Page (Dashboard Home)
 * The primary chat interface
 */

import ChatArea from '../../components/chat/ChatArea';

export default function ChatPage() {
  return <ChatArea />;
}
